import { StyleSheet } from 'react-native';
import { Color } from '../../styles';

const styles = StyleSheet.create({
  container: {
    height: 30,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#da2935'
  },
  message: {
    color: '#fff'
  }
});

export default styles;